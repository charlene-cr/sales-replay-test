# Dashboard Review Notes

## Rendering Scenario 1

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 2

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 3

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 4

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 5

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 6

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 7

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 8

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 9

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 10

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 11

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 12

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 13

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 14

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 15

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 16

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 17

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

## Rendering Scenario 18

- HTML output must escape account names and dashboard titles.
- Totals must match report payload counts.
- Follow-up rows must preserve priority for replay triage.
- Reviewers should confirm generated markup stays readable without client JavaScript.

